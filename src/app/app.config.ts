/**
* @module AppConfig
* Class store all common and shared information
*/
export class AppConfig {
    // web server url address
    public readonly apiUrl = 'http://localhost:4000';

    // chat server url address
    public readonly chatUrl = 'http://localhost:5000';

    // graphiQL server url addresses
    public readonly graphiqlURL = 'http://localhost:4000/graphql';
}
